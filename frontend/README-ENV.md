# Frontend environment variables

Create a `.env` file in this `frontend` folder with:

```
REACT_APP_API_URL=https://your-backend-domain.com/api
```

On Vercel, set this as a Project Environment Variable so builds use the correct API base URL.

